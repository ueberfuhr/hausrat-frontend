import { CalculationResult } from './calculation-result.model';

describe('CalculationResult', () => {
  it('should create an instance', () => {
    expect(new CalculationResult()).toBeTruthy();
  });
});
